import { PopinDirective } from './popin.directive';

describe('PopinDirective', () => {
  it('should create an instance', () => {
    const directive = new PopinDirective();
    expect(directive).toBeTruthy();
  });
});
